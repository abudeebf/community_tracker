<% provide(:title,@group.name)%>
 <%= render '/groups/tabup'%>
</ul>
<div class="container-fluid">  
  <div class="row-fluid">  
    <div class="span8">  
      <div class="well sidebar-nav">  
       <ul class="nav nav-list">  
        <li class="nav-header"> 
         <%= @group.name.upcase %> PROFILE </li>
         <table> <tr> <td  width="50%">
         <% if @group.photo.url=="/photos/original/missing.png" %>
 <li> <%= image_tag "community.jpg",width: "200"%></li>
  <%else %>
  <li><%= image_tag @group.photo.url,width: "200"%></li>
 <% end %>
 </td> <td> <b> Group total Activity</b>
 <% if @group.events.any? %>
  <table class="table table-striped table-bordered table-condensed center">
    <tr>
      <th><%=@group.name %> Events</th>
      <th><%=@group.name %> Total Volunteer hours</th>
    </tr>
    <td ><%= @group.events.count %></td>
      <td><%= grouphourtracker1(@group.events)%></td>
  </table>
  <%end %>
</td> </tr>
 </table>

   <li><%= @group.email %></li>
     <li><h4>Creator:</h4>
 <div> <% user=User.find(@group.memberships.find_by_member("Creator").user_id) %>
  <%= link_to  user.first_name + " " + user.last_name ,user %>
</div> </li>
<br />
<li> <h4> Address:</h4>
<div id="myCollapsibleExample"><a  data-target="#demo" data-toggle="collapse"><%= @group.address %></a>
  <input class="userid" type="hidden"  value="open"/>
<div id="demo" class="collapse"> 
<%= image_tag "http://maps.google.com/maps/api/staticmap?size=450x300&sensor=false&zoom=16&markers=#{@group.latitude}%2C#{@group.longitude}" %></div>  
</div></li> 
<br />
<li> <h4>Group Description</h4></li>
        <li> <%= @group.description %></li>

       </ul>
     </div>
     <div class="well sidebar-nav">  
       <ul class="nav nav-list">  
        <li class="nav-header"> <%=@group.name%> Events</Li>
  <ol class="users">
   <%= render @events %>
 </ol>
</div>
  </div>
    <div class="span4">  
      <div class="well sidebar-nav">  
       <ul class="nav nav-list">  
        <%if @group.gamification %>
<li class='nav-header'>Gamification is a part of this Group</p></b></li>
  <p>Gold Badge:<%=@group.gold%> hours.</p>
  <p>Silver Badge:<%=@group.silver%> hours.</p>
  <p>Bronze Badge:<%=@group.bronze%> hours.</p>
<% end %>
  <% if @users.any?%>
    <li class='nav-header'>Group Members</li>
   <li><p><b> Group has <%=@users.count%> members</b></p>
   <ol class="users">
 <%= render @users %>
 </ol>
   <% end %>
 </li>
  </ul>
   </div>
  </div>
</row>
  
    
      
</div>
</div>















































































































































































































































































































































































































































































































































































































