<div class="container">  
  <row class="span12">  
     <div class="well span8">
<h4>Please confirm the hours</h4>  
  <br/>
<%= form_for(@pastevent, :url => pastevent_path(params[:id])) do |f| %>
    <%= User.find(@pastevent.user_id).first_name + " " + User.find(@pastevent.user_id).last_name%> has requested to approve  <%= @pastevent.hours%> volunteer hours through your organization <%= @pastevent.orgname%>.
Please approve if the request is genuine. If you think that the request is not true, please disregard.If you think that the hours are not correct then please write the actual hours in the text box below.<br />
Thank you!
<br />
<label><b>Hours</b></label>
   <%= f.text_field :hours %>
<br />
<label><b>Decision</b></label>
 <% y=false %>
 <div class="controls" >
   <% ["Confirm","Reject"].each do |x| %> 
    <label class="radio inline">
    <% if x=="Confirm" %>
     <% y=true%>
      <% else %>
       <% y=false%>
       <% end %>
         <%= f.radio_button :approval, y %><%= x %> 
          <% end %> 
      </div>
      <br />
  <div class="actions">
    <%= f.submit "submit" ,class: "btn btn-large btn-primary"%>
  </div>

<%end%>
</div>
</row>
</div>